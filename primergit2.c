#include <stdio.h>
main(){
	printf(" touch crea un archivo
		vim lo edita 
		mkdir crea una carpeta
		ls muestra los archivos de la carpeta
		cd cambia la direccion de la carpeta
		clear borra el texto
		git init pone a git en la carpeta a utilizar
		git add aniade un archivo a los que se van a usar
		git commit -m <mensaje>  guarda el archivo git
		")
	return 0;
}
